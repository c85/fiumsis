name: 'fiu_dbt'
version: '1.0.0'

profile: 'fiu_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fiu_dbt:
    yt_trending_data:
      dim_video:
        +materialized: incremental
        +unique_key: video_id
        +incremental_strategy: merge
        +merge_exclude_columns: ["DW_CREATE_TS"]
      dim_channel:
        +materialized: incremental
        +unique_key: channelid
        +incremental_strategy: merge
        +merge_exclude_columns: ["DW_CREATE_TS"]
      dim_category:
        +materialized: incremental
        +unique_key: categoryid
        +incremental_strategy: merge
        +merge_exclude_columns: ["DW_CREATE_TS"]
      fact_yt_trending:
        +materialized: incremental
        +unique_key: ['video_id', 'channelid', 'categoryid']
        +incremental_strategy: merge
        +merge_exclude_columns: ["DW_CREATE_TS"]